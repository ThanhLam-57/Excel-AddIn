
<p>Nếu bạn gặp lỗi Excel mở addin nhưng không hoạt động, hãy kiểm tra:</p>
<ol>
	<li>Đảm bảo MS Windows và MS Office 365 đã được cập nhật bản mới nhất.</li>
	<li>Đảm bảo đã cài đặt trình duyệt web Edge phiên bản Chromium.</li>
	<li>Nếu vẫn không mở được addin, hãy tải về file ở dưới về máy tính và chạy để sửa lỗi.</li>
</ol>

<a class="btn btn-primary" href="<%= utils.makeUrl('/user/edge-legacy-fix/download') %>"><i class="fas fa-download"></i> Tải về file sửa lỗi</a>

