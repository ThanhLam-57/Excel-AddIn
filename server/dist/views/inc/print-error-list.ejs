<% if (errors.length > 0) { %>
	<div class="alert alert-danger">
		<% if (errors.length == 1) { %>
			<%- errors[0] %>
		<% } else { %>
			<ul class="m-0">
				<% for (const item of errors) { %>
					<li><%- item %></li>
				<% } %>
			</ul>
		<% } %>
	</div>
<% } %>